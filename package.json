{
  "name": "@godtech/cli-identity",
  "version": "0.1.0",
  "description": "Shared terminal identity and branded CLI banner renderer for gODtECH products.",
  "type": "module",
  "bin": { "godtech-identity": "dist/src/cli.js" },
  "exports": { ".": "./dist/src/index.js" },
  "types": "./dist/src/index.d.ts",
  "files": ["dist/src"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "test": "npm run build && node scripts/run-tests.mjs",
    "prepare": "npm run build",
    "prepack": "npm run build"
  },
  "keywords": ["godtech", "cli", "terminal", "branding", "developer-tools"],
  "license": "Apache-2.0",
  "repository": { "type": "git", "url": "https://github.com/gODtECH-Ctl-Create/gODtECH-CLI-Identity.git" },
  "devDependencies": { "typescript": "^5.9.3" }
}
